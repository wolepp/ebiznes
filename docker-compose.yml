version: "3.7"
services:

  backend:
    image: test-backend
    environment:
      - PLAY_SECRET_KEY
      - GOOGLE_ID
      - GOOGLE_KEY
      - FACEBOOK_ID
      - FACEBOOK_KEY
    ports:
      - "9000:9000"   # scala play

  frontend:
    image: test-frontend
    ports:
      - "80:80"   # nginx
      - "8080:8080"
